import java.util.Random;

public class Compte {
      int n;
      char typeDeCompte;
      double solde;
      int codeSecret;

    // Constructeur par défaut\\
    public Compte() {
        this.n= 999999;
        this.typeDeCompte = '';
        this.solde = 0;
    }
    public Compte(int unNumero, char unType, double unSolde) {
        this.numero = unNumero;
        this.solde = unSolde;

        // Vérification du type de compte et affectation\\
        if (typeDeCompte == 'D' || typeDeCompte == 'E') {
            this.typeDeCompte = String.typeDeCompte;
        } else {
            typeDeCom public int getNumero() {pte = ""; 
        }
   
        Code();
    }
}
}
